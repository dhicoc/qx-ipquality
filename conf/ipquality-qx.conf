# Quantumult X · 节点 IP 质量检测 (qx4.2)
# 必须长按节点运行；URL 带 ?v= 防止缓存旧脚本

[task_local]
event-interaction https://raw.githubusercontent.com/dhicoc/qx-ipquality/master/src/ipquality-qx.js?v=20260720-43, tag=节点IP质量检测, img-url=shield.lefthalf.filled.system, enabled=true

# 可选：轻量连通对比（本机 vs 节点，不加远端 TCP）
# event-interaction https://raw.githubusercontent.com/dhicoc/qx-ipquality/master/src/ipquality-qx.js?v=20260720-43, tag=IP检测·连通, img-url=shield.lefthalf.filled.system, enabled=true, argument=block=1

# 参数：
#   pure=1|0     IPPure 纯净度，默认 1
#   block=0|1|full  默认 0；1=轻量连通；full=含 check-host（易超时）
#   mask=0|1     遮盖 IP
