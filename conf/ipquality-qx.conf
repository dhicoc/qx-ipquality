# Quantumult X · 节点 IP 质量检测
# 长按节点 → 节点IP质量检测
#
# 默认：出口 IP + IPPure（较快、稳定）
# 阻断诊断：加 argument=block=1（更慢，易超时）

[task_local]
event-interaction https://raw.githubusercontent.com/dhicoc/qx-ipquality/master/src/ipquality-qx.js, tag=节点IP质量检测, img-url=shield.lefthalf.filled.system, enabled=true

# 全功能（含远端 TCP / 阻断，约 10～18 秒）
# event-interaction https://raw.githubusercontent.com/dhicoc/qx-ipquality/master/src/ipquality-qx.js, tag=IP检测·全开, img-url=shield.lefthalf.filled.system, enabled=true, argument=block=1&pure=1

# 参数：mask=0&pure=1&block=0
